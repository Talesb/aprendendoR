# R é Fracamente tipado
variavel <- 2
# 2
variavel 
#true 
variavel == 2

nome <- 'Tales B'
# Tales b
nome
 
 #Lista as variáveis já declaradas
 ls()

#Remove as variáveis já declaradas
rm(variavel)

# nome
ls()

#Remove todas as variáveis já declaradas
rm(list = ls()) 

#character(0)
ls()

#Para limpar o console
shell('cls')
